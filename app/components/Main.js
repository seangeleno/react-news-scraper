// React required here
var React = require('react');
// Including the Link component from React Router to navigate within our app without full page reloads

var Link = require('react-router').Link;

// Now for the Main component
var Main = React.createClass({render: function(){
	return (
	
)
}});
